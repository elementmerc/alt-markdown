// @altmd/components - the alt-markdown standard-library Web Components (scaffold).
//
// The richer-nine roster (callout, tabs, accordion, columns, chart, math,
// enhanced-table, diagram, embeds) lands in Phase 5, each with the mandatory
// static fallback and built as vanilla custom elements behind one small base
// class. Diagrams and embeds render only inside the Phase 3 sandbox.

export const COMPONENTS: readonly string[] = [];
